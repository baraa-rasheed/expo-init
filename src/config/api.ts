// In React Router Framework mode, API routes are served from the same origin.
// Keep VITE_API_URL to support alternate deployments if needed.
export const API_URL = import.meta.env.VITE_API_URL ?? '';

