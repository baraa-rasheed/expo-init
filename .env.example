# ---- Server ----
# Port the React Router server listens on in production (`npm run start`).
PORT=3000

# ---- Client ----
# Optional. Override the API base URL when the web UI is hosted on a
# different origin from the API. Leave empty to use same-origin requests.
VITE_API_URL=
